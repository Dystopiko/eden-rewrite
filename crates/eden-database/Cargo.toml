[package]
name = "eden-database"
version = "0.1.0"
edition = "2024"
license = { workspace = true }

[dependencies]
diesel = { workspace = true }
diesel-derive-enum = { version = "2.1.0", features = ["postgres"] }

[lints]
workspace = true
